--仅用于代码提示功能，请勿require，否则渲染功能将不可用
XCustomizedRenderer = {}

function XCustomizedRenderer.CreateRenderBatch(
    BufferUsage, indices, vertices, vertexFormats, elementLength, elementSize, needNormalize)
end

function XCustomizedRenderer.UpdateSetting(RenderFunctionID, value)
    
end

function XCustomizedRenderer.UpdateBlendSettting(BlendFunctionID, BlendFunctionValue)
    
end

function XCustomizedRenderer.RenderBatch(
    RenderHandle, shaderFolder, shaderName, textureData, uniformData, indicesCount, updateOffset, updateData)
    
end
